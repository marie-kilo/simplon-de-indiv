<<<<<<< HEAD
## 1. Qu’est‑ce que j’ai produit hier ?
- J’ai travaillé sur l’API des festivals : j’ai créé une fonction dans `clean` pour nettoyer le tableau *festivals*, j’ai ajouté la source dans `main`, et j’ai ajouté dans `collect` le script pour insérer les données.  
  J’ai aussi ajouté dans `main` une boucle pour parcourir tous les départements de France.  
  J’ai téléchargé toutes les données *festivals* dans PostgreSQL (mais je ne suis pas satisfaite du résultat, j’ai récupéré plus de données dans mes tests).
- J’ai cherché une API non‑Opendatasoft et j’ai trouvé une API pour les sports. J’ai choisi *rugby_team*.  
  J’ai fait un test dans `rugby.py` pour récupérer les 93 équipes en utilisant une clé API.  
  J’ai fait le schéma, la collecte, et ajouté la table *rugby_team* dans `main`.  
  Mais je n’ai pas obtenu un bon résultat.

## 2. Où est‑ce que j’ai bloqué, et comment je m’en suis sortie ?
- Le blocage était dans `main` : le tableau *festivals* n’a pas une colonne `insee_code`, mais `code_insee_commune`.  
  Le prof m’a expliqué comment trouver l’erreur. J’avais des erreurs dans le `select` et le `where` dans les sources, et j’ai modifié la fonction `clean_festival`.
- J’ai eu un blocage avec l’API *rugby_team* : il fallait une clé API, et je ne pouvais pas l’afficher dans le navigateur.  
  J’ai créé un compte pour obtenir la clé et j’ai trouvé comment l’utiliser en Python.  
  Ensuite j’ai eu d’autres blocages : je n’avais pas le code INSEE, seulement le code postal et la ville.  
  J’ai ajouté les codes postaux dans la table *commune*, j’ai modifié `clean` pour récupérer le code INSEE depuis PostgreSQL, et j’ai modifié `collect`, `load`, `main`.  
  L’API Rugby ne fonctionne pas avec `select` et `where`.  
  Les blocages venaient aussi du fait que l’API Rugby n’a pas de pagination et ignore `offset`.  
  J’ai essayé de parcourir *rugby_team* une seule fois.  
  Le code fonctionne, mais le résultat n’est pas encore correct.

## 3. Qu’est‑ce que je referais autrement si je recommençais ?
Je choisirais une autre API plus simple, avec un code INSEE direct.  
Ce n’est pas facile de trouver une API gratuite, publique, et qui fonctionne bien (hors Opendatasoft).  
Je voulais faire une API pour l’immobilier en France.  
Mais avec toutes ces complications, j’ai mieux compris comment fonctionnent les API.  
Pour l’API Rugby, je ferais tout dans un fichier séparé.

## 4. Qu’est‑ce qui reste flou ?
Pas de question pour l’instant.  
Je vais continuer à chercher une solution pour la table *rugby_team*, mais je dois avancer sur l’analyse.
=======
## 1. Qu’est‑ce que j’ai produit hier ?
- J’ai travaillé sur l’API des festivals : j’ai créé une fonction dans `clean` pour nettoyer le tableau *festivals*, j’ai ajouté la source dans `main`, et j’ai ajouté dans `collect` le script pour insérer les données.  
  J’ai aussi ajouté dans `main` une boucle pour parcourir tous les départements de France.  
  J’ai téléchargé toutes les données *festivals* dans PostgreSQL (mais je ne suis pas satisfaite du résultat, j’ai récupéré plus de données dans mes tests).
- J’ai cherché une API non‑Opendatasoft et j’ai trouvé une API pour les sports. J’ai choisi *rugby_team*.  
  J’ai fait un test dans `rugby.py` pour récupérer les 93 équipes en utilisant une clé API.  
  J’ai fait le schéma, la collecte, et ajouté la table *rugby_team* dans `main`.  
  Mais je n’ai pas obtenu un bon résultat.

## 2. Où est‑ce que j’ai bloqué, et comment je m’en suis sortie ?
- Le blocage était dans `main` : le tableau *festivals* n’a pas une colonne `insee_code`, mais `code_insee_commune`.  
  Le prof m’a expliqué comment trouver l’erreur. J’avais des erreurs dans le `select` et le `where` dans les sources, et j’ai modifié la fonction `clean_festival`.
- J’ai eu un blocage avec l’API *rugby_team* : il fallait une clé API, et je ne pouvais pas l’afficher dans le navigateur.  
  J’ai créé un compte pour obtenir la clé et j’ai trouvé comment l’utiliser en Python.  
  Ensuite j’ai eu d’autres blocages : je n’avais pas le code INSEE, seulement le code postal et la ville.  
  J’ai ajouté les codes postaux dans la table *commune*, j’ai modifié `clean` pour récupérer le code INSEE depuis PostgreSQL, et j’ai modifié `collect`, `load`, `main`.  
  L’API Rugby ne fonctionne pas avec `select` et `where`.  
  Les blocages venaient aussi du fait que l’API Rugby n’a pas de pagination et ignore `offset`.  
  J’ai essayé de parcourir *rugby_team* une seule fois.  
  Le code fonctionne, mais le résultat n’est pas encore correct.

## 3. Qu’est‑ce que je referais autrement si je recommençais ?
Je choisirais une autre API plus simple, avec un code INSEE direct.  
Ce n’est pas facile de trouver une API gratuite, publique, et qui fonctionne bien (hors Opendatasoft).  
Je voulais faire une API pour l’immobilier en France.  
Mais avec toutes ces complications, j’ai mieux compris comment fonctionnent les API.  
Pour l’API Rugby, je ferais tout dans un fichier séparé.

## 4. Qu’est‑ce qui reste flou ?
Pas de question pour l’instant.  
Je vais continuer à chercher une solution pour la table *rugby_team*, mais je dois avancer sur l’analyse.
>>>>>>> 364429a4caad644009b4f066c210d6e56c84f99d
