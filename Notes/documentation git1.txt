cd Desktop/cours
mkdir git_step_by_step

cd git_step_by_step

git init
ls -a
touch file_1.txt   (creat file)
(écrire dans le fichier: "echo: Bonjour" + save it 

git add file_1.txt  (Ajout du fichier à l’index)
git status *vérifier)

*(Configuration de ton identité Git)
git config --global user.name "marie-kilo"
git config --global user.email "kilomarinette@gmail.com"



*(Premier commit)
git commit -m 'file created'

*(Connexion au dépôt GitHub)
git remote add origin https://github.com/marie-kilo/git_step_by_step.git

*(Renommage de la branche master → main)
git branch -M main

*(Envoyer ton projet sur GitHub)
git push -u origin main

********************

$ cd Desktop/cours/simplon-de-indiv/

pwd (pour savoir le chemin où on est)
git init
git status
git add .
git commit -m "created"
git branch -M main
git remote add origin https://github.com/marie-kilo/simplon-de-indiv.git
git push -u origin main

*(pour suprimer tous) rm -rf .git

************

wich python

~/.pyenv/version/3.14.2/bin/python script.py (pour excuter les scripts)

~/.pyenv/version/3.14.2/bin/python script.py -m venv env

source env/bin/activate

wich python

python script.py (pour excuter le script)

wich pip
pip install pandas 


>> Desktop/cours/simplon-de-indiv/weeks/week-3/deployment-python-tuto 

touch requirements.txt  pandas ==3.0.5

>> Desktop/cours/simplon-de-indiv/weeks/week-3/deployment-python-tuto 

touch .python-version          (3.14.2)

****
pip onstall r …..

python script.py

***
ls ~/.pyenv   (pour savoir .pyenv) ~ (la racine)
pyenv install 3.14.2

****

on commence ici :

*******
0. créer dossier :deployment-python-tuto

mkdir deployment-python-tuto

git init

1-(pour créer dossier env dans la racine de docier deployment-python-tuto)

~/.pyenv/pyenv-win/versions/3.14.2/python.exe -m venv env

ls -al

2- créer fichier script.py
  touch script.py

******
[

print("Please listen and don't code for now.")


import sys

print(sys.version_info)

import pandas as pd

df = pd.DataFrame([[i for i in range(10)] for j in range(10)])

print(df)
]
*****
3- activate l'envirenement:

 source env/scripts/activate

4-savoir où python:

 where.exe python

5-executer script.py

python script.py     (pandas avec problème!) donc ...>

6-créer requirments.txt

touch requirements.tx
echo " pandas==3.0.3">requirements.txt

7- installer -r requir

pip install -r requirements.txt

-->>
*execute le script.py

python script.py


8- créer .python-version

echo "3.14.2"

9- créer .gitignore
echo " env"

10- git status  (on voit le 5 fichiers qu'on a créer)

11- git add .

git commit -m "created"
git branch -M main
git remote add origin https://github.com/marie-kilo/deployment-python-tuto.git
git push -u origin main


************
pip install requests

* ajouter dans requirements.txt :

requests==2.34.2

>>pip install - r requirements.txt

>>git status

>>git add requirements.txt
>>git commit -m "Added lib requests==2.34.2"
>> git push
