1- créer une compte Scalingo : https://auth.scalingo.com/users/sign_up
2- avoir git bach (déjà fait) 
3- CLI Scalingo ( Command Line Interface)
4- créer une clée SSH (ou réutiliser):
	a.copier
	b.ajouter sur le Dashboard Scalingo
5- Se connecter via CLI 

resources: https://doc.scalingo.com/platform/getting-started/setup-ssh-windows

dans GIT BASH:
***************
1. Check if you already have an available SSH key:
ls $HOME/.ssh

2. Create a new SSH key pair:

$ cd $HOME
$ mkdir -p ~/.ssh
$ cd .ssh
$ ssh-keygen.exe -t ed25519 (après appuier ENTER, ENTER)
( created private et public keys)

ls (pour vérifier)
id_ed25519 id_ed25519.pub  (ce sont mon clées public et privé)

**********

3. Add the public SSH key to Scalingo: lien: https://dashboard.scalingo.com/account/keys
then click : add :
et dans git bash :cat $HOME/.ssh/id_ed25519.pub
then: copy le ssh qui est dans git: 
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFT9XfI0/aMVt4mh3lUYfAz+Er5mlWVEpRlSXZIKB5Ca marieantoinettekilo

et copier dans:https://dashboard.scalingo.com/account/keys:
 SSH public key dans scalingo.

****************************

CLI: https://doc.scalingo.com/tools/cli/start
on download scalingo.exe et met le fichier dans programme
dans bash:
$ export PATH=$PATH:/c/Program\ Files/
scalingo.exe   (pour vérifier)

$ scalingo login
on ajout username ou email et le password pour scalingo
mariea.kilo ou mariantoinettekilo@gmail.com

1)*********creat application in scalingo: (déployer une application)
App name (required)
application1...creat

2)*********** Deployer un premier script 
*créer une dossier (scalongo1)
créer des fichiers:
.python-version ===> 3.14.5
script.py ====> print("Hello, Scalingo!")
requirments.txt
Procfile ====> web: python script.py

3)*********ouvrir une nouvelle terminal: PowerShell
git init
git add .
git commit -m "first commit"

on est ici==>> PS C:\Users\Utilisateur\Desktop\cours\scalingo1> 

git remote add production git@ssh.osc-fr1.scalingo.com:application1.git  (le lien de repository dans scalingo)

pour verifier les remote:
 git remote -v (montre tous les remotes).

 git push --set-upstream production master	(first time) et après (git push production)

************************
dans scalingo add_new-addond===>postgreQSL
************************
script.py===>>

import os

print(os.environ.get("DATABASE_URL", "valeur_par_defaut"))

git add .
git commit -m"first commit"
git push


on peut vérifier dans logs of application dans scalingo

***********************
********
Projet: corrige0:==>>
*******************
dans load:

DB_URL = os.environ.get("DATABASE_URL", "dbname=megabase0")

dans Procfile:==>
*************
web: python -c "import http.server, os; http.server.HTTPServer(('0.0.0.0', int(os.environ.get('PORT', 8080))), http.server.BaseHTTPRequestHandler).serve_forever()"
worker: python main.py 69
git add .
git commit -m"first commit"
git push production marie/01-07-2026:master
****************

*Pour s'arrêter l
====> Containers
====> Manage containers:
met worker : 0 ==> scale
(worker :1 ==> pour l'executer)

Addon: Postgres Dashboard:
setting:
Internet access
Force TLS connections  click: Enable 

dans l'application corrige0: ===> envirenement:

SCALINGO_POSTGRESQL_URL = postgres://corrige0_6027:8GF_yOig_l_QC1RHn2QYDAswDE7pbD7c_RVtBXUkXWZPp1ipTnbZ9ZeLJ1jzfG8D@corrige0-6027.postgresql.c.osc-fr1.scalingo-dbs.com:34644/corrige0_6027?sslmode=prefer
*********************==>
dans pgAdmin:
sur Servers: registre server:
le nom de server : corrige0_scaling (le nom de server qu'on veut)

==> connection: (ajoute: SCALINGO_POSTGRESQL_UR)
-----------------------------------------------------------------------
Host name/adress: corrige0-6027.postgresql.c.osc-fr1.scalingo-dbs.com
port : 34644
Maintenance database: corrige0_6027
Username: corrige0_6027
Password:8GF_yOig_l_QC1RHn2QYDAswDE7pbD7c_RVtBXUkXWZPp1ipTnbZ9ZeLJ1jzfG8D
************************
dans PowerShell:

PS C:\Users\Utilisateur\Desktop\cours\anonymos\groupe-sources>scalingo.exe --app les-anonymous run bash


